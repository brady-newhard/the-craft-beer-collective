<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit <%= brewery.beer || 'Craft Beer' %></title>
    <!-- <link href="./css/output.css" rel="stylesheet"> -->
    <link rel="stylesheet" href="/css/styles.css">
  </head>
  <body>
    <%- include('../partials/_navbar.ejs') %>
    <img src='/images/cbc-logo-transparent.png' class="img-size">
    <h2>Edit Your Craft Beer</h2>
    <form class="brewery-list" action="/users/<%= user._id %>/breweries/<%= brewery._id %>?_method=PUT" method="POST">
        <label for="name">Name:</label>
        <input type="text" name="name" id="name" value="<%= brewery.name %>" required />
        <label for="location">Location:</label>
        <input type="text" name="location" id="location" value="<%= brewery.location %>" required />
        <label for="link">Brewery Link:</label>
        <input type="url" id="link" name="link" value="<%= brewery.link %>" />
        <h2>Beer Details</h2>
        <label for="beer">Name:</label>
        <input type="text" name="beer" id="beer" value="<%= brewery.beer %>" required />
        <label for="type">Style:</label>
        <input type="text" name="type" id="type" value="<%= brewery.type %>" required />
        <label for="method">Method:</label>
        <select id="method" name="method">
          <option value="tap">Tap</option>
          <option value="can">Can</option>
          <option value="bottle">Bottle</option>
          <option value="other">Other</option>
      </select>
        <label for="rating">Rating:</label>
        <select id="rating" name="rating">
            <option value="🍻">🍻</option>
            <option value="🍻🍻">🍻🍻</option>
            <option value="🍻🍻🍻">🍻🍻🍻</option>
            <option value="🍻🍻🍻🍻">🍻🍻🍻🍻</option>
            <option value="🍻🍻🍻🍻🍻">🍻🍻🍻🍻🍻</option>
        </select>
        <label for="comment">Comment:</label>
        <textarea name="comment" id="comment"><%= brewery.comment %></textarea>
        <button type="submit">Submit</button>
    </form>
    <div>
      <a href="/users/<%= user._id %>/breweries"><button>Back to My Craft Beer</button></a>
    </div>
    <script>
      const menuToggle = document.getElementById('menu-toggle');
      const menu = document.getElementById('menu');
  
      menuToggle.addEventListener('click', () => {
        menu.classList.toggle('active');
      });
    </script>
  </body>
</html>